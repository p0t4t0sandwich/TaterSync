pluginManagement {
    repositories {
        gradlePluginPortal()

        // Fabric
        maven {
            name = 'Fabric'
            url = 'https://maven.fabricmc.net/'
        }
    }
}

rootProject.name = 'TaterSync'

include(
        'bukkit',
        'bungee',
        'fabric',
        'forge',
        'velocity',
        'common'
)
